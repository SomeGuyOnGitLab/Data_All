SELECT TWEETY.TWEET_COUNT tweet_bucket, COUNT(TWEETY.USER_ID) users_num
FROM (SELECT COUNT(TWEET_ID) TWEET_COUNT, USER_ID
      FROM TWEETS
      WHERE TWEET_DATE > '12/31/2021' AND TWEET_DATE <'01/01/2023'
      GROUP BY USER_ID) TWEETY
GROUP BY TWEETY.TWEET_COUNT;
